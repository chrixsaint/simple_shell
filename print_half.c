#include "shell.h"
/**
 * prints_half - This functions resets a value.
 * @str: This is a param.
 */
void prints_half(char *str)
{
	int n, l;

	l = 0;

	while (str[l] != '\0')
	{
		l++;
	}
	n = (l - 1) / 2;
}
